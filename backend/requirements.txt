fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.1
python-multipart>=0.0.9 # For file uploads
requests>=2.31.0 # Example, if calling external services
# Add other necessary libraries, e.g., for parsing specific file types (pandas, openpyxl)
